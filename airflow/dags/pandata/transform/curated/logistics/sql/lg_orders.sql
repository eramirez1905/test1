SELECT
  orders.uuid,
  orders.id,
  countries.rdbms_id,
  orders.code,
  orders.country_code,
  orders.region,
  orders.lg_city_uuid,
  orders.lg_city_id,
  orders.lg_zone_uuid,
  orders.lg_zone_id,
  orders.lg_dropoff_address_uuid,
  orders.lg_dropoff_address_id,
  orders.lg_pickup_address_uuid,
  orders.lg_pickup_address_id,
  orders.stacking_group,
  orders.is_preorder,
  orders.platform,
  orders.entity,
  orders.order_status,
  orders.tags,
  orders.capacity,
  orders.vendor_order_number,
  orders.customer_location_geo,
  orders.estimated_prep_time_in_minutes,
  orders.estimated_prep_buffer_in_minutes,
  orders.updated_prep_time_in_seconds,
  orders.hold_back_time_in_seconds,
  orders.vendor_reaction_time_in_seconds,
  orders.dispatching_time_in_seconds,
  orders.rider_reaction_time_in_seconds,
  orders.rider_accepting_time_in_seconds,
  orders.to_vendor_time_in_seconds,
  orders.expected_vendor_walk_in_time_in_seconds,
  orders.estimated_walk_in_duration_in_seconds,
  orders.estimated_walk_out_duration_in_seconds,
  orders.estimated_courier_delay_in_seconds,
  orders.estimated_driving_time_in_seconds,
  orders.rider_late_in_seconds,
  orders.vendor_late_in_seconds,
  orders.assumed_actual_preparation_time_in_seconds,
  orders.bag_time_in_seconds,
  orders.at_vendor_time_in_seconds,
  orders.at_vendor_time_cleaned_in_seconds,
  orders.vendor_arriving_time_in_seconds,
  orders.vendor_leaving_time_in_seconds,
  orders.expected_vendor_walk_out_time_in_seconds,
  orders.to_customer_time_in_seconds,
  orders.customer_walk_in_time_in_seconds,
  orders.at_customer_time_in_seconds,
  orders.customer_walk_out_time_in_seconds,
  orders.actual_delivery_time_in_seconds,
  orders.promised_delivery_time_in_seconds,
  orders.order_delay_in_seconds,
  orders.mean_delay_in_zone_in_seconds,
  orders.cod_change_for_local,
  orders.cod_collect_at_dropoff_local,
  orders.cod_pay_at_pickup_local,
  orders.delivery_fee_local,
  orders.online_tip_local,
  orders.total_value_local,
  orders.timezone,
  orders.updated_scheduled_pickup_at_utc,
  orders.promised_delivery_at_utc,
  orders.vendor_accepted_at_utc,
  orders.sent_to_vendor_at_utc,
  orders.original_scheduled_pickup_at_utc,
  orders.order_placed_at_utc,
  orders.created_date_utc,
  orders.created_at_utc,
  orders.deliveries,
  orders.porygon,
  orders.cancellation,
  orders.vendor,
FROM `{project_id}.pandata_intermediate.lg_orders` AS orders
LEFT JOIN `{project_id}.pandata_intermediate.pd_countries` AS countries
       ON orders.country_code = countries.lg_country_code
