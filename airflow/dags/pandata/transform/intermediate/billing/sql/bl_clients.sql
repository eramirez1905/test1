SELECT
  `{project_id}`.pandata_intermediate.PD_UUID(id, rdbms_id) AS uuid,
  id,
  rdbms_id,

  client_code,
  CAST(active AS BOOLEAN) AS is_active,
  CAST(automatically_invoiceable AS BOOLEAN) AS is_automatically_invoiceable,
  CAST(is_setup_confirmed AS BOOLEAN) AS is_setup_confirmed,
  bank_account_name,
  bank_account_number,
  bank_branch_name,
  bank_information,
  bank_name,
  billing_address,
  cheque_in_favour_of,
  cheque_in_favour_of_bank_account_number,
  cheque_in_favour_of_bank_name,
  cheque_in_favour_of_mailing_address,
  invoice_language,
  name,
  invoice_method AS invoice_method_type,
  payment_mode AS payment_mode_type,

  created_at AS created_at_utc,
  updated_at AS updated_at_utc,
  dwh_last_modified AS dwh_last_modified_utc,
FROM `{project_id}.pandata_raw_ml_billing_latest.client`
