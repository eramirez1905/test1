SELECT
  shifts.uuid,
  shifts.id,
  countries.rdbms_id,
  shifts.lg_rider_uuid,
  shifts.lg_rider_id,
  shifts.lg_city_uuid,
  shifts.lg_city_id,
  shifts.lg_starting_point_uuid,
  shifts.lg_starting_point_id,
  shifts.lg_zone_uuid,
  shifts.lg_zone_id,
  shifts.created_by_lg_user_uuid,
  shifts.created_by_lg_user_id,
  shifts.updated_by_lg_user_uuid,
  shifts.updated_by_lg_user_id,
  shifts.country_code,
  shifts.is_repeating,
  shifts.state,
  shifts.tag,
  shifts.vehicle_bag,
  shifts.vehicle_profile,
  shifts.deliveries_accepted_count,
  shifts.deliveries_notified_count,
  shifts.planned_shift_duration_in_seconds,
  shifts.login_difference_in_seconds,
  shifts.logout_difference_in_seconds,
  shifts.actual_working_time_in_seconds,
  shifts.actual_break_time_in_seconds,
  shifts.timezone,
  shifts.start_at_utc,
  shifts.end_at_utc,
  shifts.actual_start_at_utc,
  shifts.actual_end_at_utc,
  shifts.created_date_utc,
  shifts.created_at_utc,
  shifts.updated_at_utc,
  shifts.absences,
  shifts.actual_working_time_by_date,
  shifts.actual_break_time_by_date,
  shifts.evaluations,
  shifts.break_times,
FROM `{project_id}.pandata_intermediate.lg_shifts` AS shifts
LEFT JOIN `{project_id}.pandata_intermediate.pd_countries` AS countries
       ON shifts.country_code = countries.lg_country_code
